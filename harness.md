harness

第一层：上下文管理
1.1、角色和目标定义：模型知道自己是谁，任务是什么，成功的标准是什么
1.2、信息选择和裁剪：上下文不是越多越好，而是越相关越好
1.3、结构化组织：固定规则放哪里，当前任务放哪里，运行状态放哪里，外部证据放哪里，最好分层清楚
第二层：工具系统
2.1、给它什么工具：工具太少，能力不够；工具太多，模型会乱用
2.2、什么时候该调工具：本来不需要查的时候别乱查，该查证的时候也别硬答
2.3、结果怎么重新喂回：搜索回来的十条结果，不应该原封不动塞回去，而是要提炼、筛选、保持和任务的相关性
第三层：执行编排
3.1、理解目标->判断信息够不够，不够就去补充->继续分析生成输出->检查输出如果不满足就修正
第四层：状态与记忆
记忆分层
4.1、当前任务状态
4.2、会话中间结果
4.3、长期记忆与偏好
第五层：评估与观测
包含输出验证、环境验证、自动测试、日志和指标、错误归因
系统不仅要会做，还要知道自己有没有真的做对，比如针对同一个问题，模型回答两个不一样的答案怎么解决
第六层：约束与恢复
6.1、约束：哪些能做，哪些不能做
6.2、校验：输出前怎么检查
6.3、恢复：失败以后怎么重试、切路径、回滚到稳定状态

上下文太多，注意力不集中使用subagent
自评失真，模型会偏乐观，要工具的可验证的检测来进行验证
渐进式披露：类似Agent.md放全部东西容易注意力涣散，改成目录，具体的内容想看了再查
